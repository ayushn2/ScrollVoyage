import { AnimatedValue } from '@react-spring/animated';
import { FluidValue } from '@react-spring/shared';
import { AnimationConfig } from '@react-spring/core/dist/declarations/src/AnimationConfig';
import { PickEventFns } from '@react-spring/core/dist/declarations/src/types/internal';
import { SpringProps } from '@react-spring/core/dist/declarations/src/types';
/** An animation being executed by the frameloop */
export declare class Animation<T = any> {
    changed: boolean;
    values: readonly AnimatedValue[];
    toValues: readonly number[] | null;
    fromValues: readonly number[];
    to: T | FluidValue<T>;
    from: T | FluidValue<T>;
    config: AnimationConfig;
    immediate: boolean;
}
export interface Animation<T> extends PickEventFns<SpringProps<T>> {
}
